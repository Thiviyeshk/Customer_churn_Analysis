import pandas as pd
df=pd.read_csv("train.csv")
df.head(10)
df = df.drop(columns=[
    'Customer_ID',
    'Name',
    'Email',
    'Last_Support_Ticket'
])
df.head()
df.isnull().sum()
df=df.dropna()
df.info()
df.describe()
df.to_csv("cleaned_churn_data.csv", index=False)
df = pd.read_csv("cleaned_churn_data.csv")
df.head()
df['Login_Frequency'].unique()
df.head(40)
df['Login_Frequency'] = df['Login_Frequency'].map({
    'Daily': 4,
    'Weekly': 3,
    'Monthly': 2,
    'Rarely': 1
})
df['Login_Frequency'].unique()
df.to_csv("encoded_churn_data.csv", index=False)
df['Login_Frequency'].value_counts()
df.to_csv("encoded_churn_data.csv", index=False)
churn_rate = df['Churn'].mean() * 100
print("Churn Rate:", churn_rate)
import matplotlib.pyplot as plt

df['Churn'].value_counts().plot(kind='bar')

plt.title("Customer Churn Distribution")
plt.xlabel("Churn")
plt.ylabel("Number of Customers")

plt.show()
import seaborn as sns
sns.boxplot(x='Churn', y='Daily_Usage_Mins', data=df)
plt.title("Daily Usage vs Churn")
plt.show()
sns.boxplot(x='Churn', y='Login_Frequency', data=df)
plt.title("Login Frequency vs Churn")
plt.show()

sns.boxplot(x='Churn', y='Account_Age_Days', data=df)
plt.title("Account Age vs Churn")
plt.show()

df.columns
x=df[['Account_Age_Days',
      'Login_Frequency',
      'Daily_Usage_Mins']]
y=df['Churn']
x.head()
y.head()
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier

df = pd.read_csv("encoded_churn_data.csv")

x=df[['Account_Age_Days','Login_Frequency','Daily_Usage_Mins']]
y=df['Churn']
x_train, x_test, y_train, y_test = train_test_split(
    x,y, test_size=0.2, random_state=42
)

model = RandomForestClassifier()
model.fit(x_train, y_train)

from sklearn.metrics import accuracy_score, precision_score, recall_score

y_pred = model.predict(x_test)

accuracy = accuracy_score(y_test, y_pred)
print("Accuracy:", accuracy)

precision = precision_score(y_test,y_pred)
print('Precision:', precision)

recall = recall_score(y_test,y_pred)
print("Recall:",recall)

df['Churn_Prediction'] = model.predict(x)

df.head()

df['Rish_Level'] = df['Churn_Prediction'].map({
    1:'High Risk',
    0:'Safe'
})

df.head()
df.to_csv("churn_dashboard_data.csv", index=False)

df = pd.read_csv("churn_dashboard_data.csv")

df.head()

df.insert(0, "Customer_ID", ["CUST" + str(i).zfill(3) for i in range(1, len(df)+1)])

df.head()

df.to_csv("churn_dashboard_data_updated.csv", index=False)

df.head()
















